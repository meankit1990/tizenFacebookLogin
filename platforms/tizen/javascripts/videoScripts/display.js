var Display = {
	bufferDiv : null,
};

Display.init = function() {
	this.bufferDiv = document.getElementById("buffer");
};

Display.showBufferingDialog = function() {
	if (this.bufferDiv) {
		//show
	}
};

Display.hideBufferingDialog = function() {
	if (this.bufferDiv) {
		//hide
	}
};

Display.showErrorDialog = function() {
		//show
};

Display.hideErrorDialog = function() {
		//hide
};

Display.showLoadingDialog = function() {
		//show
};

Display.hideLoadingDialog = function() {
		//hide
};


